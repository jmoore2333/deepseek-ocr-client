{
  "mcpServers": {
    "deepseek-ocr": {
      "command": "python",
      "args": ["${CLAUDE_PLUGIN_ROOT}/../backend/mcp_server.py"],
      "env": {
        "DEEPSEEK_OCR_URL": "${DEEPSEEK_OCR_URL:-http://127.0.0.1:5000}"
      }
    }
  }
}
